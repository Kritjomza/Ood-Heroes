# Phase 4 Asset Replacement Guide

Use `docs/assets/phase-4-asset-manifest.json` as the authoritative 75-ID contract. Generate isolated sources into the paths in `art-prompts/14_google_flow_file_manifest.md`, review each source, assemble only the six hero and five VFX atlases programmatically, validate dimensions/frame order/alpha, then place approved final static files or assembled atlases at their exact manifest destination. Each monster remains one static 96 × 96 left-facing WebP and is mirrored at runtime. Pending GDD assets have no production path.
